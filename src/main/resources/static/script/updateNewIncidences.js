window.setInterval(
    function(){
    // Sección de código para modificar el DIV
    // $("#miDiv").text(variable);

    // Ejemplo: Cada dos segundos se imprime la hora
    console.log(Date());
  }
  // Intervalo de tiempo
,2000);